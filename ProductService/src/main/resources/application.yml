#
#spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/productdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update # You can set this to validate, create, create-drop, none, etc.
#    properties:
#      hibernate:
#        show_sql: true # Set to true to show SQL statements in logs
#        format_sql: true # Set to true to format SQL statements in logs
#        dialect: org.hibernate.dialect.MySQL8Dialect # Specify the MySQL dialect version
